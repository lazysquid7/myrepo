git init
git add .
git status
git commit -m "first commit message"
git remote add origin "repo http link"
git push origin master
git push -u origin master  //then we can just use git push if we use -u 
